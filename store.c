void store(unsigned char * buffer, int * RESULT, int * REGISTERS)
{
    printf("RESULT: %d\n", *RESULT);
    int instruction = buffer[0] >> 4;

    switch(instruction)
    {
        case 1: // add
        case 2: // and
        case 3: // 
        case 4: // 
        case 5: 
        case 6:

            break;
    }
}